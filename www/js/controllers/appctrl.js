angular.module('appctrl.controllers', [])

.controller('AppCtrl', function($scope, $window, $state, $rootScope, $ionicModal, $timeout) {

	$scope.$on( "$ionicView.enter", function( scopes, states ) {
		// if( states.stateName == "app.story_list" ) {
		// }
	});
});
